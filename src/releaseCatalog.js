// BreakMetric release catalog coordinator v1.
(function(root){"use strict";const api={};api.load=async function(){const loader=root.BreakMetricDataLoader;if(!loader)throw new Error("Data loader unavailable");const registry=await loader.loadJson("data/releases/index.json");const releases=[];for(const r of registry.releases||[]){const formats=await loader.loadJson(r.formats_file);releases.push({...r,formats:(formats.formats||[]).map(f=>({id:f.id,name:f.name,status:f.status,analysis_unit:f.analysis_unit||null,analysis_data:f.analysis_data||null}))});}return {default_release_id:registry.default_release_id,releases};};api.release=(catalog,id)=>catalog?.releases?.find(r=>r.id===id)||null;api.readyFormats=(release)=>release?.formats?.filter(f=>f.status==="ready")||[];api.canAnalyze=(release,formatId)=>Boolean(api.readyFormats(release).some(f=>f.id===formatId)&&release?.collector_configs?.[formatId]);api.releaseOptions=catalog=>(catalog?.releases||[]).map(r=>({id:r.id,name:r.name,status:r.status,enabled:api.readyFormats(r).length>0}));api.pendingReason=release=>release?.status==="pending"?"Data package is still being verified.":null;api.pendingReason=release=>release?.status==="pending"?"Data package is still being verified.":null;root.BreakMetricReleaseCatalog=Object.freeze(api);})(typeof window!=="undefined"?window:globalThis);
