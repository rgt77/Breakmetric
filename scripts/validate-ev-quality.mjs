import fs from "node:fs";const files=fs.readdirSync("data/derived").filter(x=>x.includes("ev-contribution-provenance")&&x.endsWith(".json")),issues=[];for(const file of files){const d=JSON.parse(fs.readFileSync("data/derived/"+file,"utf8"));for(const r of d.entries||[]){const n=Number(r.expected_copies_per_case);if(!Number.isFinite(n)||n<0)issues.push(file+":invalid-expected-copies");const v=Number(r.market_value_usd);if(r.market_value_usd!=null&&(!Number.isFinite(v)||v<0))issues.push(file+":invalid-market-value");}}process.stdout.write(JSON.stringify({schema_version:1,model:"ev-data-quality-v1",files:files.length,status:issues.length?"failed":"passed",issues},null,2)+"\n");if(issues.length)process.exitCode=1;