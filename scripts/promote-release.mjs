import fs from "node:fs";import {promotionAllowed} from "./lib/release-automation.mjs";const args=process.argv.slice(2),i=args.indexOf("--package");if(i<0)throw new Error("package argument required");const file=args[i+1],pkg=JSON.parse(fs.readFileSync(file,"utf8"));if(!promotionAllowed(process.cwd(),pkg))throw new Error("Release package is not eligible for promotion");pkg.status="ready";fs.writeFileSync(file,JSON.stringify(pkg,null,2)+"\n");process.stdout.write(JSON.stringify({status:"ready",product_id:pkg.product_id,format_id:pkg.format_id},null,2)+"\n");