import fs from "node:fs";import path from "node:path";export const componentPaths=(id,format="hobby")=>({format:"data/formats/"+id+".json",base_checklist:"data/checklists/"+id+"-base.json",insert_checklist:"data/checklists/"+id+"-inserts.json",autograph_checklist:"data/checklists/"+id+"-autographs.json",odds:"data/odds/"+id+"-"+format+".json",inventory:"data/derived/"+id+"-"+format+"-ev-eligible-inventory-v1.json",provenance:"data/derived/"+id+"-"+format+"-ev-contribution-provenance-v1.json",observations:"data/collection/"+id+"-market-observations-v1.json"});export const exists=(root,p)=>fs.existsSync(path.join(root,p));export function readiness(root,paths){return Object.fromEntries(Object.entries(paths).map(([k,p])=>[k,exists(root,p)]));}export function nextMissing(root,paths){return Object.entries(paths).filter(([,p])=>!exists(root,p)).map(([k,p])=>({component:k,path:p}));}export function automationStatus(root,pkg){const missing=nextMissing(root,pkg.required_components||{});return {product_id:pkg.product_id,format_id:pkg.format_id,missing,complete:missing.length===0,fail_closed:pkg.fail_closed===true};}
export const safeReleaseId=id=>/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(String(id||''));
export const packagePath=(id,format='hobby')=>'data/releases/'+id+'-'+format+'.json';
export const scopeKey=(id,format)=>id+'::'+format;
export const allComponentsPresent=(root,paths)=>nextMissing(root,paths).length===0;
export const promotionAllowed=(root,pkg)=>pkg.fail_closed===true&&allComponentsPresent(root,pkg.required_components||{})&&Object.values(pkg.readiness||{}).every(Boolean);