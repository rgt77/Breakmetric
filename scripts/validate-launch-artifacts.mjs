import fs from "node:fs";const issues=[];for(const p of ["data/validation/launch-readiness-v1.json","docs/launch-runbook.md","scripts/validate-launch-readiness.mjs",".github/workflows/codebase-audit.yml"])if(!fs.existsSync(p))issues.push("missing:"+p);const read=p=>fs.readFileSync(p,"utf8"),r=JSON.parse(read("data/validation/launch-readiness-v1.json"));if(!read("docs/launch-runbook.md").includes("Rollback"))issues.push("rollback-missing");if(!read("docs/launch-runbook.md").includes("Smoke checks"))issues.push("smoke-checks-missing");if(!read(".github/workflows/codebase-audit.yml").includes("validate-launch-readiness.mjs"))issues.push("ci-launch-gate-missing");if(r.release_candidate===true&&r.blockers?.some(x=>x.required_for_launch))issues.push("blocked-candidate-promoted");process.stdout.write(JSON.stringify({schema_version:1,model:"launch-artifact-validation-v1",status:issues.length?"failed":"passed",issues},null,2)+"\n");if(issues.length)process.exitCode=1;