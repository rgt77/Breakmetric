import fs from "node:fs";const read=p=>fs.readFileSync(p,"utf8"),html=read("index.html"),catalog=JSON.parse(read("data/products/catalog.json")),issues=[];const required=["1 · Release","2 · Box type","3 · Team","4 · Player","Spot price","productGrid","formatGrid","teamGrid","playerGrid","selectionSummary","flowProgress"];for(const x of required)if(!html.includes(x))issues.push("missing-ui-contract:"+x);if(catalog.products.length<3)issues.push("release-catalog-too-small");if(!html.includes('button.disabled = item.status !== "ready"'))issues.push("pending-release-not-disabled");if(!html.includes('selectedPlayer'))issues.push("optional-player-state-missing");if(!html.includes('syncUrlState'))issues.push("url-state-missing");if(!html.includes('BreakMetricStorage'))issues.push("selection-persistence-missing");if(!html.includes('selectionCoordinator'))issues.push("async-selection-guard-missing");process.stdout.write(JSON.stringify({schema_version:1,model:"frontend-flow-validation-v1",status:issues.length?"failed":"passed",release_count:catalog.products.length,issues},null,2)+"\n");if(issues.length)process.exitCode=1;