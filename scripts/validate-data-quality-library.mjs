import fs from "node:fs";const src=fs.readFileSync("scripts/lib/data-quality.mjs","utf8"),issues=[];for(const key of ["validateCards","validateChecklistCount","validateSubjects","validateTeams","validateOdds","validateOddsUniqueness","validateOddsMagnitude","validateMarketRows","validateInventoryRows","validateContributionRows","validateScope","qualityResult"])if(!src.includes(key))issues.push("missing:"+key);if(!src.includes("fail_closed:true"))issues.push("fail-closed-missing");process.stdout.write(JSON.stringify({schema_version:1,model:"data-quality-library-validation-v1",status:issues.length?"failed":"passed",issues},null,2)+"\n");if(issues.length)process.exitCode=1;