import fs from "node:fs";import path from "node:path";import {automationStatus} from "./lib/release-automation.mjs";const root=process.cwd(),dir=path.join(root,"data/releases"),rows=[];for(const name of fs.readdirSync(dir).filter(x=>x.endsWith(".json")&&x!=="index.json")){const pkg=JSON.parse(fs.readFileSync(path.join(dir,name),"utf8"));if(pkg.model!=="release-data-package-v1")continue;rows.push({...automationStatus(root,pkg),package:"data/releases/"+name,status:pkg.status});}process.stdout.write(JSON.stringify({schema_version:1,model:"release-automation-status-v1",packages:rows},null,2)+"\n");